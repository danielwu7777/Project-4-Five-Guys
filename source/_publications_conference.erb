<div id="conference_publications">
  <h2>Conference Papers</h2>
  <% data.conference_papers.papers.each do |paper| %>
    <div class="publication">
        <span class="authors">
          <% paper.authors.each do |author| %>
            <%= author %>,
          <%end %>
        </span>
        <span class="paper_title"><%= paper.title %></span>,
        <span class="paper_publisher"><%= paper.publisher %></span>,
        <span class="paper_date"><%= paper.date %></span>
        <% if paper.file != nil %>
          <a class="paper_file" href="<%= paper.file %>">[pdf]</a>
        <% end %>
    </div>
  <% end %>
</div>